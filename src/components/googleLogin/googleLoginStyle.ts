import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  logInWithGoogle: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
    marginVertical: 20,
  },
  logInWithGoogleText: {
    fontWeight: 600,
  },
});
